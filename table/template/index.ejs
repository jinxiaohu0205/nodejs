<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/index.css">
    <script src="/js/angular.min.js"></script>
    <script src="/js/angular-route.min.js"></script>
    <script src="/js/angular-animate.min.js"></script>
    <script src="/js/index.js"></script>
</head>
<body>
<div class="box" ng-app="myapp" ng-controller="ctrl">
    <div class="header">学生信息系统</div>
    <table class="table-bordered">
        <tr>
            <th>姓名</th>
            <th>年龄</th>
            <th>性别</th>
            <th>班级</th>
            <th>操作</th>
        </tr>
        <tr ng-repeat="item in data">
            <td>
                <input type="text" ng-model="item.name" ng-blur="blur(item.id,item.name,'name')">
            </td>
            <td>
                <input type="text" ng-model="item.age" ng-blur="blur(item.id,item.age,'age')">
            </td>
            <td>
                <input type="text" ng-model="item.sex" ng-blur="blur(item.id,item.sex,'sex')">
            </td>
            <td>
                <input type="text " ng-model="item.classes" ng-blur="blur(item.id,item.classes,'classes')">
            </td>
            <td>
                <a href="javascript:;" ng-click="del(item.id)" class="btn btn-warning">删除</a>
            </td>
        </tr>
    </table>
    <a href="#!/add" class="add glyphicon glyphicon-plus-sign"></a>
    <div style="width: 20vw;height: 30vh;position: fixed;left:40%;top:20%;background:rgba(0,0,0,0.5);border-radius: 10px;text-align: center;line-height: 100px;color:#f00;" ng-show="isshow==true">操作成功</div>
</div>
</body>
</html>
<!--修改 params  -->
<!--ng-show=“isshow”-->
<!--$timeout(function(){},2000)-->
<!--添加 insetId-->